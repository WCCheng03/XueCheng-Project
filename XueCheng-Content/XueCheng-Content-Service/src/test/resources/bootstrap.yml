#微服务配置
spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 172.30.85.69:8848
      discovery:
        namespace: dev
        group: xuecheng-project
      config: #配置文件相关配置
        namespace: dev
        group: xuecheng-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: feign-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true

  profiles:
    active: dev   #环境名
#  datasource:
#    driverClassName: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://172.30.85.69:3306/xc_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
#    username: root
#    password: Wwc050619

# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
